import React from 'react';
import {ScrollView} from "../../../components";

function UserInfo () {
    return (
        <ScrollView scrollY>
            <div>用户信息</div>
        </ScrollView>
    );
};

export default UserInfo;
